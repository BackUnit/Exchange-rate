package com.example.financecalculators.ui.theme

import androidx.compose.ui.graphics.Color

// Grounded, professional palette - Slate, Charcoal, Muted Blue/Grey
val DarkBackground = Color(0xFF1A1D23)  // Deep charcoal
val DarkSurface = Color(0xFF242832)      // Lighter charcoal
val DarkSurfaceVariant = Color(0xFF2D3240)  // Muted slate
val SurfaceHighlight = Color(0xFF363C4A)  // Subtle highlight

// Muted primary colors
val Primary = Color(0xFF6B7C93)          // Muted blue-grey
val PrimaryVariant = Color(0xFF546075)   // Darker muted blue
val OnPrimary = Color(0xFFFFFFFF)

// Secondary accent - subtle green for positive financial data
val Secondary = Color(0xFF5B8C6F)        // Muted sage green
val SecondaryVariant = Color(0xFF4A755C)

// Text colors
val OnDarkBackground = Color(0xFFE8ECF1)  // Soft white
val OnDarkSurface = Color(0xFFE8ECF1)
val OnSurfaceMuted = Color(0xFF9AA3B2)    // Muted text

// Financial accents (subtle, not rainbow)
val AccentGreen = Color(0xFF5B8C6F)       // Muted green for gains
val AccentBlue = Color(0xFF6B7C93)        // Muted blue
val AccentOrange = Color(0xFFB87D4B)      // Muted orange
val AccentRed = Color(0xFF9B5C5C)         // Muted red for losses

// Card gradients (subtle)
val CardGradientStart = Color(0xFF2D3240)
val CardGradientEnd = Color(0xFF242832)